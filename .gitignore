# local planning notes — never committed
PLAN.md

# secrets never live on disk (op run resolves infra/env/.env.template),
# but ignore stray root env files defensively (anchored so the committed
# template under infra/env/ is unaffected)
/.env
/.env.*

# node / nuxt
node_modules
.nuxt
.output
dist
.data

# trigger.dev
.trigger

# clickhousectl project-local state (local server/postgres data dirs)
.clickhouse

# seed resume state (make seed)
.seed-progress.json

# misc
.DS_Store
*.log
